# FretSense 项目目标书

> 本文档是 FretSense 项目的**最终北极星**。
> 后续所有代码、模块、命名、文档都必须服务于本文件中定义的目标与叙事。
> 任何与本文件冲突的实现，要么修改实现，要么修改本文件 —— 不允许背离。

---

## 一、项目定位（一句话）

**FretSense 是一个基于认知诊断与多 Agent 协同的自适应吉他教学系统，通过感知学生认知状态与谱面视觉信号，提供长程个性化学习闭环。**

---

## 二、Title 与 Slogan

- **正式 Title**：`FretSense: 基于认知诊断与多 Agent 协同的自适应吉他教学 Agent`
- **副标题（简历用）**：Multi-Agent · Cognitive Diagnosis · Multi-Modal Perception
- **Slogan**：*Sense the learner. Sense the score. Teach what matters next.*

> Title 设计意图：
> - `Fret`（吉他品柱）+ `Sense`（感知）—— 同时呼应「感知学生状态」与「感知谱面视觉」双重多模态语义。
> - 学术化方法论组合（认知诊断 + 多 Agent + 多模态）作为简历钩子，避免「个性化教学 Agent」这种烂大街表达。

---

## 三、核心用户痛点（来自吉他自学者真实诉求）

| 痛点 | 现状 | 我们的方案 |
|---|---|---|
| 不知道练得对不对 | 网课无反馈 | Tutor Agent 即时反馈 + 可选音频诊断 |
| 不知道下一步学什么 | 教程线性单一 | Planner Agent 基于 θ 动态生成 |
| 同一个错反复犯 | 缺乏错因追踪 | Diagnose Agent 维护错因图谱 |
| 看不懂谱子 | 谱子格式杂乱、和弦不熟 | **Perceptual Module：PDF 谱子 → 结构化和弦序列** |
| 千篇一律，容易放弃 | 难度不自适应 | IRT 驱动难度调节，不卡死也不无聊 |

**真痛点 + 真方案 = 项目立足的产品根基。任何脱离痛点的技术都属于炫技，不写。**

---

## 四、系统架构（反 ReAct / 反 Workflow）

### 4.1 架构图

```
                         ┌─────────────────────────────────────┐
                         │   Learner Cognitive State (θ)        │
                         │   多维能力向量（IRT 驱动）           │
                         │   节奏 / 技巧 / 乐理 / 记忆力 / ...  │
                         └─────────────────┬───────────────────┘
                                           │ 注入
        ┌────────────┬─────────────────────┼─────────────────────┬────────────┐
        ▼            ▼                     ▼                     ▼            ▼
  ┌──────────┐  ┌──────────┐         ┌──────────┐         ┌──────────┐  ┌──────────┐
  │Perceptual│  │ Diagnose │ ◄────── │ Planner  │ ──────► │  Tutor   │  │  Critic  │
  │  Agent   │  │  Agent   │         │  Agent   │         │  Agent   │  │  Agent   │
  │(谱面/音频)│  │(错因归因)│         │(路径规划)│         │(教学输出)│  │(质量评估)│
  └────┬─────┘  └────┬─────┘         └────┬─────┘         └────┬─────┘  └────┬─────┘
       │             │                    │                    │             │
       └─────────────┴────────► Skill Library ◄─────────────────┘             │
                                  (可复用教学技能)                             │
                                          ▲                                    │
                                          │                                    │
                                  Reflection Loop ◄──────────────────────────┘
                                  (评估教学效果，更新 θ，沉淀新 Skill)
```

### 4.2 为什么不是 ReAct / 不是 Workflow

| 维度 | ReAct | Workflow | **FretSense** |
|---|---|---|---|
| 决策驱动 | LLM 思维链 | 预定义流程 | **学生认知状态 θ** |
| Agent 形态 | 单 Agent 串行 | 节点固定 | **多 Agent 异构协作** |
| 状态管理 | 上下文窗口 | 节点内 state | **持久化向量 + 闭环更新** |
| 自我修复 | 无 | 无 | **Critic + Reflection 双闭环** |
| 长程一致性 | 弱 | 中 | **强（θ 跨会话持久化）** |

> **核心论点（面试可用）**：ReAct 适合状态空间小、流程标准化、延迟敏感的场景；Multi-Agent + 状态机适合状态空间大、决策依赖多源信号、长程一致性要求高的场景。**FretSense 是后者的典型代表。**

---

## 五、核心模块设计

### 5.1 Cognitive State Layer（认知状态层）

- 数据结构：`θ = [节奏能力, 技巧熟练度, 乐理理解, 曲目记忆力, ...]` n 维向量
- 建模方法：项目反应理论（IRT, Item Response Theory）
  - 每次练习视为一道「测试题」，根据答题结果反向更新 θ
  - 比简单打分更能区分「会但失误」和「真不会」
- 持久化：跨会话存储，每次对话开始时注入 prompt

### 5.2 Multi-Agent 协作层

| Agent | 职责 | 输入 | 输出 |
|---|---|---|---|
| **Perceptual Agent** | 多模态感知 | PDF/图片/音频 | 结构化和弦序列、节奏特征 |
| **Diagnose Agent** | 错因归因 | 学生练习记录 | 错因标签 + 相似 case 聚类 |
| **Planner Agent** | 教学路径规划 | θ + 学习目标 | 下一步教学动作（调用哪个 Skill） |
| **Tutor Agent** | 教学内容生成 | Skill + 学生画像 | 自然语言教学输出 |
| **Critic Agent** | 质量评估 | Tutor 输出 | 评分 + 改进建议（不达标触发 Reflection） |

### 5.3 Skill Library（自演化技能库）

- **设计灵感**：Voyager 论文的 Skill 沉淀机制
- **Skill 标准接口**：
  ```python
  class Skill:
      name: str                          # 如 "teach_C_major_scale"
      precondition: Dict[str, float]     # θ 触发阈值
      action: Callable                   # 教学动作函数
      expected_outcome: Dict[str, float] # θ 预期变化
  ```
- **自演化机制**：Reflection Loop 中识别新场景 → Critic 验证 → 沉淀为新 Skill

### 5.4 Context Harness（三层上下文脚手架）

| 层级 | 内容 | 注入时机 |
|---|---|---|
| L1 系统层 | Agent 角色、教学法原则、安全约束 | 启动时一次性 |
| L2 学生层 | θ 向量、错题历史、偏好、长期记忆摘要 | **运行时直查注入**（参考美团） |
| L3 会话层 | 当前对话窗口、即时反馈 | 每轮对话动态 |

### 5.5 Memory System（分层记忆，借鉴 hello-agents）

| 记忆类型 | FretSense 中的映射 |
|---|---|
| Working Memory | 当前练习上下文 |
| Episodic Memory | 历次练习记录、对话历史 |
| Semantic Memory | 乐理知识图谱、和弦字典 |
| **Perceptual Memory** | **谱面视觉特征、音频特征**（多模态扩展点） |

### 5.6 Multi-Modal Perceptual Module（多模态感知模块）

> 这是 FretSense 名字里 "Sense" 的实指落点。

**输入支持**：
- PDF 谱子（六线谱 / 简谱 / 五线谱）
- 谱子图片（拍照上传）
- （未来）练习音频片段

**处理流水线**：
```
PDF/Image → 页面拆分 → VLM 识别（GPT-4V / Qwen-VL）
         → 和弦序列结构化 [Am, F, C, G, ...]
         → 节奏与小节信息
         → 写入 Perceptual Memory
         → Planner Agent 据此规划教学动作
```

**与美团策略模式钩子**：复用美团多源数据采集的策略模式架构 —— PDF 解析、图片解析、音频解析作为可插拔策略，统一汇聚到 Perceptual Memory。

---

## 六、美团实习钩子映射表

> **每一个技术点都必须能挂到美团经历，这是简历叙事的最高纪律。**

| 美团原始工作 | FretSense 对应模块 | 钩子叙事 |
|---|---|---|
| 全局长期记忆「异步预生成 + 运行时直查」读写分离 | 学习者画像异步生成 + 运行时注入 prompt | 「美团客服延迟敏感所以读写分离，吉他练习更敏感所以同样架构」 |
| Kafka 双延迟消息（5min/30min）触发摘要 | 短时反馈（单段练习后）+ 长时反思（每日总结）双触发 | 「将客服时间窗口改造为教育场景的事件窗口」 |
| 策略模式并行采集 IM/隐私号/对话/外呼 | 策略模式并行采集 PDF/图片/音频/对话 | 「数据源换了，模式不变」 |
| 按 sessionId/bu+subBu 分组聚合 | 按 userId/skill_dimension 分组聚合 θ 更新 | 「分组维度从业务线改为能力维度」 |
| **单体 ReAct 收敛**（Multi-Agent → ReAct，3~5 次降至 2 次） | **反向叙事**：吉他场景必须用 Multi-Agent | 「美团让我建立了架构选型框架；吉他是反向验证的最佳场景」**【核心王炸钩子】** |
| 自进化引擎：三路子 Agent 并行拉 badcase/goodcase/配置 | Diagnose Agent 三路并行拉 错题/优等生路径/Skill 库 | 「架构原样复用，场景不同」 |
| 主 Agent 相似 case 配对 + 结构化归因 | Diagnose Agent 错因聚类 + 归因 | 同上 |
| 历史 badcase 回归 + 通过率达标审批 | 教学策略改动后历史会话回归测试 | 「自修复闭环必须有回归保障」 |
| 沉淀为审查规则反哺上游 | 沉淀为 Skill Library 新条目反哺 Planner | 「数据飞轮思想直接迁移」 |

### 6.1 京东 / 百度的辅助挂钩点（次要）

- **京东**：若做练习音频上传流水线 → 挂 BLMOVE 可靠投递 + 布隆过滤器去重
- **百度**：Lesson 生命周期状态机（参考 analysisjob 设计）+ Skill 元数据本地存储（参考工具元数据迁移 + 双重检查锁）

---

## 七、简历级技术亮点（4 条）

```
- 设计基于 IRT 认知诊断驱动的多 Agent 协同架构（Perceptual / Diagnose / Planner / Tutor / Critic），
  实现教学动作的状态机闭环决策；相比 ReAct 单 Agent 在长程教学路径准确率上提升 XX%。

- 实现自演化 Skill Library（Voyager 风格），Agent 运行中沉淀可复用教学技能 N 类，
  叠加三层 Context Harness（系统/学生/会话）+ 独立 Critic Agent 评估器，
  解决长程对话中上下文漂移与质量不可控问题。

- 构建分层记忆系统（工作/情景/语义/感知）+ 异步预生成 + 运行时直查的读写分离架构，
  支撑跨会话学习画像持续演化，借鉴美团 WOWService 长期记忆设计经验。

- 落地多模态感知模块（PDF 谱子 / 图片 / 音频）：基于策略模式的可插拔解析器
  统一输出至 Perceptual Memory，VLM 解析端到端准确率 XX%。
```

---

## 八、面试叙事脚本（一段话版）

> 「在美团 WOWService 我做了三件事：搭建订单与用户双维度长期记忆、把 80% 高并发场景从 Multi-Agent 收敛到 ReAct、设计自进化引擎修复 badcase。
>
> 这三件事让我建立了三个判断：**长期记忆必须读写分离、Agent 架构必须按状态空间选型、Agent 系统必须有自修复闭环。**
>
> FretSense 这个项目，本质是我把这三个判断在一个**反向场景**里做系统性验证 —— 吉他教学的状态空间比客服更复杂、用户耐心比客服长、反馈比客服稀疏。我想知道：当场景反过来时，这些判断是否依然成立？
>
> 项目跑下来的结论是：**长期记忆架构跨场景普适；Agent 选型框架经得起反向验证（吉他必须用 Multi-Agent）；自修复闭环在数据稀疏场景下需要叠加回归测试机制。**」

---

## 九、开发路线图

### MVP（第 1 阶段，最小可运行闭环）
- [ ] 基础 Multi-Agent 框架（Diagnose / Planner / Tutor / Critic）
- [ ] θ 向量数据结构 + 简化 IRT 更新逻辑
- [ ] Skill Library 最小集（5~10 个手写 Skill）
- [ ] Context Harness 三层注入
- [ ] CLI 对话入口

### Phase 2（差异化能力）
- [ ] Reflection Loop + 自演化 Skill 沉淀
- [ ] 分层记忆系统（参考 hello-agents 实现）
- [ ] 异步预生成 + 运行时直查（钩子核心）

### Phase 3（多模态扩展，FretSense 名字落点）
- [ ] PDF 谱子解析 → 和弦结构化
- [ ] 图片谱子识别
- [ ] （可选）练习音频特征提取

### Phase 4（数据飞轮，简历亮点收官）
- [ ] Critic 自动诊断 badcase
- [ ] 历史会话回归测试
- [ ] Skill 反哺机制

---

## 十、铁律（开发过程中必须遵守）

1. **每个模块都必须能回答**「这个能挂到美团哪个工作上？」—— 答不上来就重新设计或砍掉。
2. **不写炫技代码**。所有技术选型必须能从「用户痛点 / 简历叙事 / 钩子叙事」三者之一推导出来。
3. **Title 不许改**（除非业务方向重大变化）。FretSense 是简历钩子的视觉锚点。
4. **永远以 Title + Slogan + 简历四条 + 叙事脚本 为最高优先级**。代码实现是为了让这五个东西可信。

---

*本文档是项目的最终目标，不是过程文档。请勿在此追加日志、进度、TODO。*
